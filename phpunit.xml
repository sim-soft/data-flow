<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
         displayDetailsOnTestsThatTriggerDeprecations="true"
         displayDetailsOnTestsThatTriggerWarnings="true"
         failOnRisky="true"
         failOnWarning="true">
    <testsuites>
        <testsuite name="Unit">
            <directory>tests</directory>
            <exclude>tests/Integration</exclude>
            <exclude>tests/Properties</exclude>
        </testsuite>
        <testsuite name="Integration">
            <directory>tests/Integration</directory>
        </testsuite>
        <testsuite name="Properties">
            <directory>tests/Properties</directory>
        </testsuite>
    </testsuites>
    <!--
        Coverage outputs are requested on the command line by `composer coverage`
        rather than configured here: a <coverage> report block makes PHPUnit warn
        when no driver is installed, and failOnWarning above would turn every
        ordinary `composer test` run red. See RELEASING.md.
    -->
    <source>
        <include>
            <directory>src</directory>
        </include>
    </source>
</phpunit>
